/**
 * Pre-launch switches. Flip to true when the given area is ready for the
 * public; until then it is admin-only (see ADMIN_ONLY_PREFIXES in src/proxy.ts
 * and `adminOnly` courses in src/lib/courses) and its promotions are hidden.
 */
export const PROGRAM_LAUNCHED = false;
